services:
  app:
    build: .
    image: preconceive:latest
    container_name: preconceive
    ports:
      - "5001:5000"
    environment:
      - PORT=5000
      - ADDON_SECRET=${ADDON_SECRET}
    volumes:
      - ./addons:/app/addons:ro
    restart: unless-stopped

